<form method="post" action="{{ path('phone_number_delete', {'id': phone_number.id}) }}" onsubmit="return confirm('Jeste li sigurni da želite pobrisati ovu stavku?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_number.id) }}">
    <button class="btn">{{ 'site.delete'|trans }}</button>
</form>
