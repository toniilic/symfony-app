<form method="post" action="{{ path('phone_number_delete', {'id': phone_number.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ phone_number.id) }}">
    <button class="btn">Delete</button>
</form>
