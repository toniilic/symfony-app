{% extends 'base.html.twig' %}

{% block title %}PhoneNumber{% endblock %}

{% block body %}
    <h1>PhoneNumber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone_number.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ phone_number.type }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ phone_number.number }}</td>
            </tr>
            <tr>
                <th>IsHidden</th>
                <td>{{ phone_number.isHidden ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('phone_number_index') }}">back to list</a>

    <a href="{{ path('phone_number_edit', {'id': phone_number.id}) }}">edit</a>

    {{ include('phone_number/_delete_form.html.twig') }}
{% endblock %}
