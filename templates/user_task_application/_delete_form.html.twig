<form method="post" action="{{ path('task_application_delete', {'id': task_application.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_application.id) }}">
    <button class="btn">Delete</button>
</form>
